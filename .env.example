# Backend base URL for production or staging
# Example: https://api.seu-dominio.com
VITE_API_URL=http://localhost:3000 

# Local dev proxy target. If set, Vite will proxy
# requests to /api/* to this target during `npm run dev`.
# Example: http://localhost:3000
VITE_PROXY_TARGET=5173
